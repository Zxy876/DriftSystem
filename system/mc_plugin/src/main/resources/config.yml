backend:
  baseUrl: "http://127.0.0.1:8000"
  timeoutMs: 3000

rhythm:
  moveCooldownMs: 2500   # 玩家移动触发剧情的节流, 插件侧
  chatCooldownMs: 1200   # 输入框触发剧情的节流
  minSpeed: 0.02         # 速度阈值

ui:
  useActionBar: true     # true=ActionBar显示剧情；false=聊天框显示
