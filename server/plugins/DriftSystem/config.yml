backend:
  # 你的 FastAPI 后端地址（本地 dev 用 8000）
  baseUrl: "http://localhost:8000"

chat:
  # 玩家普通聊天前缀触发 AI（例如：!你好）
  aiPrefix: "!"

  # 玩家说“/saytoai”也会调用 AI
  enableSayToAICommand: true

world:
  # 允许通过自然语言创建新剧情
  allow_story_creation: true